/*
KLineService — собирает свечи OHLC за разные интервалы (1m,5m,...).

Назначение:
- на каждом тике собирать максимум/минимум/открытие/закрытие/объем за интервал
- выдавать готовые свечи для аналитики и истории

Параметры:
- поддерживаемые интервалы configurable (e.g., 60s, 300s)
- хранение последних N свечей на символ (ring buffer)

API (словесно):
- onTrade(trade) -> обновить текущую свечу для интервала
- getOhlc(symbol, interval, n) -> List<Candle>

Тесты:
- последовательные маленькие трейды формируют корректную свечу
*/
