/*
Contract: Tickable
Роль: объекты, которые реагируют на каждую итерацию времени (тик).

API:
- onTick(int tickIndex) -> void
  Вызывается из Ticker в порядке, описанном в Шаге 5.
  Требования: быстрое выполнение; тяжелые расчеты — делегировать; не блокировать поток.

Потенциальное поведение:
- BotCompany.onTick -> вызывает стратегию и может инициировать OrderSpec
- MarketService.onTick -> пересчет цен
*/
