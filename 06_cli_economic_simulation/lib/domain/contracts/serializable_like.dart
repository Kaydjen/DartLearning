/*
Contract: SerializableLike

API:
- Map<String,dynamic> toMap()
  Должен содержать поля для восстановления сущности, _schema и _version.

- static fromMap(Map m) -> Self (реализуется в конкретном классе)
  Должен валидировать вход и бросать/возвращать ошибку в случае несоответствия.

Замечания:
- Не сериализовать transient/derived поля
- Использовать идентификаторы (id) для ссылок между сущностями, а не вложенные объекты
*/
